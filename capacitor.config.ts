import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.panhadev.teamyearng.app',
  appName: 'PanhaProfile',
  webDir: 'dist'
};

export default config;
